<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD Interview</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; }
        .glass { background: rgba(24, 24, 27, 0.8); backdrop-filter: blur(12px); }
    </style>
</head>
<body class="bg-zinc-950 text-zinc-100 min-h-screen">
    <nav class="glass border-b border-zinc-800 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-6">
            <div class="flex justify-between h-20">
                <div class="flex items-center gap-3">
                    <span class="text-xl font-bold tracking-tighter">CRUD InterView</span></span>
                </div>
                <div class="flex items-center gap-8">
                    <div class="hidden md:flex gap-6 text-sm font-medium">
                        <a href="/laundry" class="text-zinc-400 hover:text-white transition">Transactions</a>
                        <a href="/customers" class="text-zinc-400 hover:text-white transition">Customers</a>
                    </div>
                    <a href="/logout" class="text-sm font-semibold bg-zinc-800 hover:bg-zinc-700 px-5 py-2.5 rounded-full border border-zinc-700 transition">Logout</a>
                </div>
            </div>
        </div>
    </nav>
    <main class="max-w-7xl mx-auto px-6 py-12">
        {{{body}}}
    </main>
</body>
</html>